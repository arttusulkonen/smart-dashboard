{
  "name": "smart-dashboard",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && npx firebase deploy --only hosting"
  },
  "devDependencies": {
    "@vitejs/plugin-legacy": "^5.0.0",
    "firebase-tools": "^13.0.0",
    "terser": "^5.27.0",
    "vite": "^5.0.0"
  },
  "dependencies": {
    "firebase": "^8.10.1"
  }
}
